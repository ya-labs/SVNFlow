{
  "name": "svnflow",
  "version": "0.0.1",
  "description": "Desktop app for Git+SVN workflow management in corporate environments",
  "main": "dist/main/index.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build": "tsc -p tsconfig.build.json && node ./scripts/copy-renderer-assets.mjs",
    "dev": "tsc -p tsconfig.build.json --watch",
    "app": "npm run build && electron .",
    "app:dev": "npm run build && electron ."
  },
  "keywords": [
    "git",
    "svn",
    "workflow",
    "corporate"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "electron": "^41.7.1",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  }
}
